{
  "name": "a2-cicd-assignment",
  "version": "1.0.0",
  "description": "CI/CD assignment A2 for Jenkins pipeline",
  "main": "index.js",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^17.0.0"
  }
}
